export interface Prize {
    prizeId: string;
    name: string;
    description: string;
    // 添加其他必要的字段
  }
  
  export interface PrizeDetail {
    prizeDetailId: string;
    prizeId: string;
    // 添加其他必要的字段
  }
  